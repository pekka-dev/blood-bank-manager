import React from 'react';
import {render, fireEvent, waitFor} from "@testing-library/react"
import App from "../components/App";

describe("<App />", () => {
    test("shows App component on screen", () => {

    })
})
